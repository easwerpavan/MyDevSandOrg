public class variable {
    
    public static string name='c2n';
    public  static void display(){
       system.debug(name);
    
    
   list<Account> acclist=[SELECT Id, Name, Phone, AccountNumber FROM Account];
        system.debug(acclist);
}
}